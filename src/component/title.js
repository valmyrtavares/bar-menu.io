import React from "react";
import "../assets/styles/Title.css";

const Title = ({ mainTitle }) => {
  return (
    <div className="tete">
      <h3>{mainTitle}</h3>
    </div>
  );
};
export default Title;
